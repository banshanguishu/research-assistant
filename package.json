{
  "name": "research-assistant",
  "version": "0.1.0",
  "private": true,
  "description": "A from-scratch research assistant agent built with TypeScript.",
  "type": "module",
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "check": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.8.0"
  },
  "dependencies": {
    "openai": "^6.33.0"
  }
}
